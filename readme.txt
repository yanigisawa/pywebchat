Simple web-based long polling chat server. Designed to support only 2 users, 
so scale wasn't a consideration.


EXPECTED ENVIRONMENT VARIABLES:

AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY
AWS_BUCKET_NAME

